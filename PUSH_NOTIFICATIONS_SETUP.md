# إعداد إشعارات دبّر

## 1. تثبيت قاعدة البيانات

شغّل ملف `sql/push-notifications.sql` في Supabase SQL Editor مرة واحدة. الملف ينشئ جداول اشتراكات الأجهزة، تفضيلات المستخدمين، وسجل منع تكرار الإشعارات.

## 2. إضافة متغيرات البيئة

أضف المتغيرات التالية إلى بيئة النشر:

```env
VAPID_SUBJECT=mailto:your-email@example.com
VAPID_PUBLIC_KEY=...
VAPID_PRIVATE_KEY=...
```

لتوليد زوج مفاتيح جديد محليًا بعد تثبيت الحزم:

```bash
pnpm exec web-push generate-vapid-keys --json
```

ضع `publicKey` في `VAPID_PUBLIC_KEY` و`privateKey` في `VAPID_PRIVATE_KEY`. المفتاح الخاص لا يوضع في ملفات الواجهة أو المستودع.

تظل المتغيرات الحالية مثل `SUPABASE_URL` و`SUPABASE_SERVICE_ROLE_KEY` و`CRON_SECRET` مطلوبة كما هي.

## 3. النشر

بعد نشر المشروع، افتح الداشبورد على HTTPS، ادخل إلى تبويب «حسابي»، واضغط «تفعيل» في كارت «إشعارات دبّر». المتصفح سيطلب الإذن، وبعد الموافقة يتم حفظ اشتراك الجهاز في Supabase.

الإشعارات تعمل لكل جهاز/متصفح على حدة. إيقافها من جهاز لا يوقفها تلقائيًا على جهاز آخر.

## 4. ما الذي يرسل الإشعارات؟

- التذكير اليومي: يظهر الساعة 8 صباحًا بتوقيت القاهرة إذا لم توجد مصروفات مسجلة في اليوم الحالي.
- ملخص نهاية اليوم: يظهر الساعة 12:00 صباحًا بتوقيت القاهرة عن اليوم السابق، ويعرض كل العمليات، عددها، الإجمالي، وأكثر فئة صرفًا. يمكن إيقافه منفصلًا من إعدادات الإشعارات.
- الملخص الأسبوعي: يظهر صباح السبت بعد تلخيص آخر سبعة أيام، إذا كان الخيار مفعّلًا.
- تنبيه الميزانية: يصدر بعد تسجيل أو تعديل مصروف عندما يصل إجمالي الميزانية الشهرية أو ميزانية فئة إلى النسبة المحددة، مرة واحدة لكل شهر ولكل ميزانية.

يعمل الكرون صباحًا، وكذلك عند منتصف الليل، عبر جدولين يوميين في `vercel.json`. سجلات `push_notification_runs` تمنع تكرار نفس الإشعار، بينما أخطاء الاشتراكات المنتهية 404/410 تعطل الاشتراك تلقائيًا.
