# منظومة أتمتة رسائل البنوك والمحافظ في دبّر

## ما تم تنفيذه

يستقبل المشروع الآن رسائل البنوك والمحافظ عبر `POST /api/sms-webhook`، ويتحقق من `token` الخاص بالمستخدم، ثم يطابق اسم المرسل مع قائمة البنوك والمحافظ المدعومة، ويحلل النص عبر Groq/Llama من خلال `lib/groq.js`. بعد التحليل تُحفظ الحركة في Supabase، وتظهر مباشرة في صفحة `dabbar-bank-movements.html`.

البنية الحالية تستخدم Vercel Serverless Functions بدلاً من Next.js App Router؛ وهي تؤدي نفس دور endpoint في Next.js من دون إعادة بناء المشروع القائم. المسار الموحد هو `/api?route=...`، بينما Vercel يتيح أيضًا المسار المباشر حسب إعدادات المشروع.

## Payload الـWebhook

```json
{
  "token": "TOKEN_FROM_DABBAR",
  "sender": "CIB",
  "text": "تم خصم 250.00 EGP من بطاقتك لدى ..."
}
```

يمكن أيضاً إرسال `bank` بدلاً من `sender` عند الحاجة. يجب استخدام HTTPS وعدم وضع التوكن في نص ظاهر أو مشاركته مع أي شخص.

## مسار المعالجة

1. البحث عن المستخدم من خلال `profiles.sms_webhook_token`.
2. رفض الطلب إذا كان التوكن غير صحيح أو إذا أوقف المستخدم الاستقبال.
3. مطابقة المرسل مع `lib/bank-senders.js` لتحديد البنك أو المحفظة.
4. استدعاء `classifyMessage()` لاستخراج نوع الحركة والمبلغ والعملة والوصف.
5. حفظ الحركات المحايدة في `financial_events`، وحفظ المصروفات البنكية في `expenses` مع بيانات البنك في `metadata`.
6. عرض كل النتائج في صفحة **الحركات البنكية**، مع تمييز السحب والإيداع والتحويل والمراجعة المطلوبة.

## قاعدة البيانات

شغّل الملفات التالية مرة واحدة في Supabase SQL Editor:

- `sql/sms-webhook.sql` لإضافة Token وحالة الاستقبال والعداد اليومي إلى `profiles`.
- `sql/financial-recording-repair.sql` إذا كان جدول `financial_events` قديماً ولا يحتوي حقول المصدر والبيانات الوصفية.

## Android

التجربة الأساسية الموصى بها هي تطبيق MacroDroid من متجر Google Play. يتم تنزيل ماكرو مخصص من زر واحد، ثم استيراده وتفعيله داخل MacroDroid. الماكرو يلتقط رسائل SMS البنكية التي تحتوي كلمات العملات ويرسلها إلى الـWebhook. صفحة إعدادات البنوك تُظهر رابط متجر Google Play، ورابط تحميل الماكرو المخصص، وتوجل تشغيل الاستقبال.

لا يحتاج العميل إلى تثبيت أي تطبيق مخصص من خارج المتجر. يجب منح MacroDroid الصلاحيات التي يطلبها النظام فقط، مع تفعيل الماكرو يدوياً بعد الاستيراد.

## iPhone

Apple تسمح بتشغيل Personal Automation من نوع Message تلقائياً في الإصدارات التي تعرض خيار `Ask Before Running`، لكن إعدادات النظام والإجراءات نفسها قد تفرض تأكيداً. كما لا يمكن إنشاء Shortcut عام من الخادم يحتوي Token خاصاً بكل مستخدم. لذلك يوفر المشروع صفحة `public/app/ios-shortcut-setup.html` التي تعرض للمستخدم المسجل رابط الـWebhook والتوكن الخاص به وتشرح إعداد Automation يدوياً مرة واحدة.

هذا يحقق أقل احتكاك ممكن دون الادعاء بأن iPhone يستطيع قراءة كل الرسائل في الخلفية مثل Android. رابط الإعداد يظهر من شاشة **البنوك والمحافظ** ومن شاشة الإدخال الخارجي.

## نموذج Groq المقترح

يستخدم المشروع متغيرات البيئة الموجودة في `lib/config.js`:

```text
GROQ_API_KEY
GROQ_TEXT_MODEL
```

ويطلب من النموذج JSON يحتوي على `transactions`. القواعد الموجودة في `classifyMessage()` تعالج صيغ `EGP` و`جنيه مصري` و`ج.م` والعملات الأجنبية، وتحول الأرقام المنطوقة إلى أرقام، وتفصل المصروف عن السحب والإيداع والتحويل.

## مفاضلة خيارات التشغيل

| الطريقة | المفاضلة | التكلفة | تعقيد الإعداد |
|---|---|---:|---:|
| تطبيق دبّر Android + Webhook الحالي | يعمل في الخلفية ويدعم الربط الخاص بكل مستخدم | منخفضة بعد إعداد الخادم | متوسط |
| MacroDroid + Webhook | الخيار الرسمي الحالي؛ يستورد ماكرو مخصصاً ويحتاج موافقة المستخدم على التفعيل والصلاحيات | منخفضة | متوسط إلى مرتفع |
| iPhone Shortcuts Automation | لا يحتاج APK، لكن Apple قد تطلب تأكيداً ولا يضمن قراءة صامتة لكل SMS | منخفضة | متوسط |

الاختيار الافتراضي في المشروع هو **تطبيق Android المخصص** لأنه أكثر ثباتاً في الاستقبال الخلفي. أما iPhone فله مسار Shortcuts موثق وآمن، وليس ملف `.shortcut` مزيفاً أو رابط iCloud غير موجود.

## اختبار يدوي للـWebhook

بعد تشغيل SQL وتفعيل الاستقبال من لوحة التحكم:

```bash
curl -X POST "https://YOUR_DOMAIN/api/sms-webhook" \
  -H "Content-Type: application/json" \
  -d '{"token":"USER_TOKEN","sender":"CIB","text":"تم خصم 250 EGP من حسابك"}'
```

الاستجابة المتوقعة:

```json
{"ok":true,"recorded":1,"bank":"CIB"}
```

إذا كان المرسل غير مدعوم، تكون الاستجابة `200` مع `skipped: true` لمنع استهلاك طلبات الذكاء الاصطناعي على الرسائل العادية.

## ملاحظات أمان وتشغيل

- التوكن سري ويجب تدويره يدوياً عند الاشتباه بتسريبه.
- يوجد حد يومي افتراضي قدره 80 رسالة لكل مستخدم في `sms-webhook.js`.
- يجب ضبط `PUBLIC_BASE_URL` على النطاق الإنتاجي الصحيح.
- لا تعتمد على نص الرسالة وحده لتحديد البنك؛ اسم المرسل هو المصدر الأول، مع إمكانية تمرير `bank` من الجهاز.
- الحركات التي لا يمكن تصنيفها بثقة لا تُسجل كمصروف تلقائياً، وتذهب إلى **تحتاج مراجعة**.

مرجع Apple: [Enable or disable a personal automation](https://support.apple.com/guide/shortcuts/enable-or-disable-a-personal-automation-apd602971e63/ios).
