# تنفيذ Global-ready في دبّر

## ما تم تنفيذه

أضيفت طبقة `src/lib/globalContext.js` لتوحيد الدولة واللغة والعملة والـlocale والمنطقة الزمنية، مع defaults آمنة تحافظ على المستخدم المصري الحالي: `EG / ar / EGP / Africa/Cairo`.

أضيفت Migration في `src/sql/global-context.sql` تضيف إعدادات المستخدم، وتضيف `currency_code` إلى المصروفات والديون والفواتير والأهداف وأصناف الفواتير. هذا يمنع إعادة تفسير العمليات القديمة عند تغيير عملة المستخدم.

أضيف endpoint جديد هو `src/api/user-context.js` لقراءة وتحديث إعدادات المستخدم بعد التحقق من جلسة Supabase وربط Telegram، مع التحقق من اللغة والرموز الأساسية.

تم ربط Global Context باستجابة `api/dashboard-data.js`، وأضيفت بطاقة إعدادات في `public/app/dabbar-dashboard-full.html` لاختيار الدولة وكودها واللغة والعملة والمنطقة الزمنية. بعد الحفظ يعاد تحميل الداشبورد.

تم تعديل تسجيل المصروفات والديون والفواتير لحفظ `currency_code` وتنسيق رسائل التأكيد حسب العملة. كما صار تنسيق مبالغ الداشبورد ديناميكيًا، ويتغير اتجاه الصفحة إلى LTR للإنجليزية وRTL للعربية.

## خطوة ضرورية قبل النشر

في Supabase SQL Editor شغّل الملف كاملًا:

```text
src/sql/global-context.sql
```

شغّله بعد إنشاء الجداول الأساسية `users`, `expenses`, `debts`, `invoices`, `invoice_items`, و`goals`. بعد ذلك انشر نسخة المشروع وأعد تسجيل الدخول.

## الفحوصات

نجح فحص الصياغة لكل ملفات `api/` و`lib/` وعددها 33 ملفًا، ونجح فحص JavaScript المضمن في الداشبورد، ونجح اختبار `normalizeGlobalContext` و`formatMoney` للـEGP وEUR والتحقق من القيم غير الصالحة.

## حدود هذه المرحلة

هذه المرحلة تجهز البنية وتضيف إعدادات المستخدم وتنسيقًا أساسيًا. لم تتم ترجمة كل النصوص الثابتة داخل صفحات HTML أو كل رسائل Telegram إلى الإنجليزية بعد؛ ما زالت هذه خطوة لاحقة. كذلك لا يوجد تحويل عملات تلقائي، وهو مقصود: كل عملية تحفظ عملتها الأصلية ولا تُحوّل دون مصدر سعر صرف موثوق.
