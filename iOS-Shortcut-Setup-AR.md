# إعداد اختصار آيفون (Apple Shortcuts) لتسجيل مصروف سريع في دبّر

ملاحظة: آبل مش بتسمح بإنشاء ملف Shortcut جاهز برمجيًا من برة تطبيق Shortcuts نفسه
(ده احتياط أمني من آبل، مش قيد تقني تقدر تلفه). يعني في مرة واحدة بس، حد
(انت أو أي حد عندك آيفون) يعمل الاختصار من جوه تطبيق Shortcuts، وبعدين يعمله "مشاركة"
كرابط، والرابط ده يفضل شغال لأي حد يدوس عليه بعد كده.

## خطوات إنشاء الاختصار (تتعمل مرة واحدة بس)

1. افتح تطبيق **Shortcuts** على أي آيفون.
2. دوس **+** لعمل اختصار جديد.
3. دوس **Add Action** وابحث عن **Ask for Input**.
   - Prompt: اكتب مصروفك في دبّر
   - Input Type: Text
4. دوس **Add Action** تاني وابحث عن **URL**.
   - في حقل الرابط اكتب:
     `https://www.dabbar.online/app/dabbar-quick-add.html?text=`
   - بعد ما تخلص من كتابة الرابط، دوس على أيقونة **Provided Input** (الجزء الأزرق
     اللي طلع من خطوة Ask for Input) وضيفه في آخر الرابط، عشان يبقى شكل الرابط:
     `https://www.dabbar.online/app/dabbar-quick-add.html?text=[Provided Input]`
5. دوس **Add Action** تالت وابحث عن **Open URLs** (أو **Open in Safari**).
   - اختار الـ URL اللي عملته فوق كمدخل ليه.
6. سمّي الاختصار (من الإعدادات فوق): **إدخال سريع - دبّر**
   - تقدر كمان تحط أيقونة/لون مميز ليه.
7. دوس **Done**.

## مشاركة الاختصار مع المستخدمين

1. من شاشة Shortcuts الرئيسية، دوس مطولًا على الاختصار اللي عملته → **Share**.
2. اختار **Copy iCloud Link**.
3. الرابط ده حطه جوه التطبيق (في زرار "ثبّت اختصار آيفون 📱").

## تجربة المستخدم النهائية

1. المستخدم يدوس الرابط مرة واحدة بس → آيفون يفتح Shortcuts → يدوس **Add Shortcut**.
2. من ساعتها، يقدر:
   - يدوس على الاختصار من الشاشة الرئيسية أو من تطبيق Shortcuts.
   - أو ينده عليه بالاسم: **"Hey Siri, إدخال سريع دبّر"**.
3. هتظهرله نافذة صغيرة من النظام يكتب فيها المصروف (زي "دفعت 50 جنيه قهوة").
4. يدوس **Done** → آيفون يفتح صفحة دبّر لحظة، تحلل النص، وتوريله مراجعة سريعة
   يأكدها بضغطة واحدة.

## شرط أساسي

المستخدم لازم يكون **مسجل دخول في دبّر قبل كده على نفس الآيفون** (حتى لو مرة واحدة
بس من Safari)، عشان صفحة `dabbar-quick-add.html` تلاقي جلسة الدخول بتاعته وتقدر
تسجل المصروف من غير ما تطلب تسجيل دخول تاني.

## ملاحظة عن Dictate (لو عايز صوت بدل الكتابة)

لو عايز الإدخال يبقى بالصوت بدل الكتابة، في خطوة 3 استبدل **Ask for Input** بـ
**Dictate Text** — نفس الفكرة بالظبط، بس آيفون هيسجل صوت ويحوله لنص عن طريق Siri
قبل ما يبعته لدبّر.

---

# ثانيًا: أتمتة البنوك والمحافظ (زي MacroDroid بالظبط بس على آيفون)

آيفون بيدعم أتمتة اسمها **"Personal Automation"** بتتفعّل تلقائي لما توصلك رسالة SMS
من رقم/اسم معيّن، وممكن تشتغل **صامتة تمامًا من غير ما تسألك أي تأكيد** (نوع
"Message" من الأنواع اللي آبل بتسمحلها تشتغل تلقائي حسب مستندات آبل الرسمية).

⚠️ محتاج تعمل **اختصار منفصل لكل بنك/محفظة** عندك (يعني لو عندك CIB وفودافون كاش،
هتعمل اتنين اختصارات، كل واحد بنفس الخطوات بس بمرسل مختلف).

## الخطوات

1. افتح تطبيق **Shortcuts** ← تاب **Automation** (تحت) ← **+** أعلى يمين ← **Create
   Personal Automation**
2. من قايمة الأنواع اختار **Message**
3. في خانة **Sender**: اكتب اسم/رقم مرسل رسائل البنك (زي "CIB" أو رقم بنك مصر
   اللي بيوصلك منه الرسائل عادة)
4. دوس **Next**
5. دوس **Add Action** وابحث عن **Get Contents of URL**
6. حط الرابط: `https://www.dabbar.online/api/sms-webhook`
7. دوس على الرابط نفسه لتظهر خيارات إضافية ← **Method: POST**
8. **Request Body: JSON** ← ضيف 3 مفاتيح:
   - `token`: التوكن بتاعك (موجود جاهز في صفحة "حسابي" داخل التطبيق، نفس التوكن
     المستخدم في أتمتة أندرويد)
   - `sender`: اسم البنك (زي "CIB")
   - `text`: دوس على الحقل واختار **Shortcut Input** من قايمة المتغيرات
9. دوس **Next** ← قفّل **Ask Before Running** ← فعّل **Run Immediately**
10. تأكد إن **Notify When Run** مقفولة (عشان تشتغل في الخلفية من غير إشعار مزعج)
11. **Done**

كرر نفس الخطوات (1 لـ 11) لكل بنك أو محفظة إلكترونية عندك.

---

# ثالثًا: المشاركة من أي تطبيق (Share Sheet)

## أ) مشاركة نص (سهلة ومباشرة)

استخدم نفس اختصار "الإدخال السريع" من أول الملف، بس في إعداده غيّر خطوة **Ask for
Input** إلى:

1. دوس **+** بجوار الاختصار من تطبيق Shortcuts نفسه (مش من الشاشة الرئيسية) لفتحه
   للتعديل
2. من إعدادات الاختصار (أيقونة الـ (i) أعلى الشاشة) فعّل **"Show in Share Sheet"**
   وحدد نوع المدخل: **Text**
3. دلوقتي أي نص تحدده في أي تطبيق (واتساب مثلًا) وتدوس **Share** ← هيظهرلك اسم
   الاختصار في القايمة ← تختاره ← هيفتحلك صفحة الإدخال السريع بالنص جاهز

## ب) مشاركة صوت أو صورة فاتورة (محتاجة اختصار منفصل، عشان مش مربوطة بجلسة تسجيل الدخول)

الاختصار ده بيبعت مباشرة لسيرفر دبّر من غير ما يفتح المتصفح خالص، فمحتاج نفس فكرة
التوكن اللي في أتمتة البنوك فوق.

1. Shortcuts ← **+** لعمل اختصار جديد ← سمّه "مشاركة دبّر"
2. من إعدادات الاختصار (أيقونة الـ (i)) فعّل **"Show in Share Sheet"** وحدد
   الأنواع: **Images, Audio, Text**
3. ضيف Action: **Get Contents of URL**
4. الرابط: `https://www.dabbar.online/api/quick-add-webhook`
5. **Method: POST** ← **Request Body: JSON**
6. ضيف المفاتيح حسب نوع المشاركة:
   - `token`: التوكن بتاعك (نفس توكن أتمتة البنوك)
   - لو **صورة فاتورة**: مفتاح `imageBase64` ← اختار **Shortcut Input** ← حوّلها
     أول حاجة بـ Action اسمه **Base64 Encode** قبل الـ URL Action
   - لو **تسجيل صوتي**: نفس الفكرة بمفتاح `audioBase64` (برضو محتاج **Base64
     Encode** الأول) + مفتاح `mimeType` بقيمة `audio/m4a`
   - لو **نص**: مفتاح `text` ← Shortcut Input مباشرة من غير تحويل
7. **Done**

من ساعتها، أي حد يشير صورة فاتورة أو تسجيل صوتي أو نص من أي تطبيق (المعرض،
واتساب، الميكروفون...) ويختار "مشاركة دبّر"، هتتسجل مباشرة من غير ما يفتح
التطبيق أو المتصفح خالص.

## شرط أساسي للأتمتة والمشاركة (البنود التانية والتالتة)

لازم تفعّل خانة "أتمتة SMS" من صفحة "حسابي" داخل التطبيق الأول (نفس الخانة
المستخدمة لأندرويد) — التوكن نفسه بيشتغل للبنوك وللمشاركة مع بعض.

