# استعادة كلمة المرور — دبّر

تمت إضافة زر «نسيت كلمة المرور؟» في صفحة تسجيل الدخول.

## إعداد Supabase مرة واحدة

من Supabase افتح **Authentication → URL Configuration**، ثم اضبط:

- **Site URL:** رابط التطبيق الأساسي على Vercel، مثل `https://your-app.vercel.app`
- **Redirect URLs:** أضف مسار صفحة التسجيل:

```text
https://your-app.vercel.app/app/dabbar-onboarding.html?mode=recovery
```

استبدل `your-app.vercel.app` بالدومين الحقيقي للمشروع.

## طريقة الاستخدام

المستخدم يضغط «نسيت كلمة المرور؟»، يكتب الإيميل، ثم يفتح الرسالة في Inbox أو Spam. رابط الرسالة يفتح نموذج «اختار كلمة مرور جديدة»، وبعد الحفظ يستطيع تسجيل الدخول من جديد.

لا يعرض التطبيق ما إذا كان الإيميل موجودًا أم لا؛ تظهر رسالة عامة لحماية خصوصية الحسابات.

## قرار رقم الهاتف

لم يتم تحويل التسجيل إلى رقم الهاتف بعد. التنفيذ الآمن يحتاج تفعيل Phone Auth في Supabase ومزوّد SMS وOTP، مع تحديد صيغة أرقام الدولة وتكلفة الرسائل. حاليًا يظل الإيميل هو طريقة الحساب، مع استعادة كلمة المرور عبر الإيميل.
