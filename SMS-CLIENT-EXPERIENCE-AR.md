# تجربة العميل: تفعيل تسجيل البنك تلقائيًا عبر SMS

## مستخدم Android

1. يدخل العميل إلى موقع **دبّر** ويسجّل الدخول.
2. يفتح **حسابي** ثم يختار **البنوك والمحافظ**.
3. يرى بوضوح أن ميزة تسجيل SMS التلقائي متاحة لأجهزة **Android فقط**.
4. يضغط **حمّل تطبيق دبّر لـ Android (APK)**.
5. يفتح ملف APK ويضغط **تثبيت**. إذا طلب الهاتف السماح بالتثبيت من المتصفح أو الملفات، يختار السماح لهذه المرة.
6. يفتح تطبيق **دبّر — تتبع البنك**؛ ويكون توكن الحساب مملوءًا تلقائيًا من الموقع، ثم يضغط **حفظ التوكن**.
7. يضغط **تفعيل إذن الإشعارات**، ويفعّل إذن الوصول للإشعارات لتطبيق دبّر من إعدادات Android.
8. يرجع إلى الموقع أو التطبيق ويفعّل **الاستقبال التلقائي**.
9. بعد ذلك، عندما تصل رسالة إشعار بنكية من بنك أو محفظة مدعومة، يقرأ التطبيق الإشعار ويرسله بأمان إلى دبّر، ثم يحاول تصنيف العملية. المصروف يظهر في المصروفات، والدخل يظهر في قسم الدخل، والحركات غير الواضحة تظهر للمراجعة.

## مستخدم iPhone

يظهر له تنبيه واضح بأن **التسجيل التلقائي من SMS غير متاح على iPhone**. لا تظهر له أزرار تنزيل أو تفعيل تطبيق Android. يمكنه استخدام التسجيل اليدوي داخل دبّر أو اختصار iPhone إذا كان مفعّلًا.

## التسجيل اليدوي للدخل والمصروف

يختار العميل من كارت التسجيل نوع العملية أولًا:

- يختار **مصروف**، ثم يكتب مثلًا: `صرفت 75 جنيه أكل`. بعد المراجعة والتأكيد تُحفظ العملية في قسم المصروفات.
- يختار **دخل**، ثم يكتب مثلًا: `قبضت 5000 جنيه مرتب`. بعد المراجعة والتأكيد تُحفظ العملية في قسم الدخل.

اختيار النوع أصبح فعليًا ويُرسل إلى الخادم، وليس مجرد تغيير لون للزر.

## ملاحظة إصلاح قاعدة البيانات

إذا كانت قاعدة Supabase قديمة ولم تُشغّل عليها جداول `financial_events` وأعمدة `currency_code`، يجب تشغيل الملف التالي مرة واحدة من Supabase SQL Editor:

`sql/financial-recording-repair.sql`

بعد تشغيله، يعاد نشر المشروع ثم يُجرّب تسجيل دخل ومصروف جديدان للتأكد من ظهورهما في القسمين الصحيحين.

## كيف يتم تصنيف رسالة البنك؟

يستقبل تطبيق Android اسم المرسل ونص الإشعار فقط، ثم يمرّر الرسالة إلى دبّر. أولًا تتم مطابقة اسم المرسل مع قائمة البنوك والمحافظ المدعومة، مثل CIB وNBE وبنك مصر وفودافون كاش واتصالات كاش وإنستاباي. إذا كان المرسل غير معروف، تُتجاهل الرسالة ولا تُرسل للتصنيف.

بعد ذلك يقرأ المصنّف نص الرسالة ويفحص كلمات وسياق العملية. رسالة خصم من مطعم أو اشتراك تُعامل كمصروف، ورسالة مرتب أو بيع أو تحصيل تُعامل كدخل، والسحب والإيداع والتحويل تُحفظ كحركة بنكية محايدة. التحويل الغامض إلى شخص أو من شخص يحصل على علامة «تحتاج مراجعة» بدل التخمين.

المصروف الواضح يُحفظ في جدول المصروفات، والدخل الواضح يُحفظ في جدول `financial_events` بنوع `income` واتجاه `inflow`، والحركات البنكية المحايدة تظل في `financial_events` ولا تدخل في إجمالي المصروفات. عند مراجعة حركة غامضة يمكن للمستخدم تحويلها إلى مصروف أو دخل أو دين.

## شعار البنك أو المحفظة

يحفظ النظام مع الحركة `bank_key` و`bank_label` واسم المرسل. في شاشة «الحركات البنكية» يظهر بجوار كل حركة badge بهوية البنك أو المحفظة، مثل `CIB` أو `NBE` أو `VF`، ويظهر الاسم الكامل أسفله. للبنوك والمحافظ غير المعروفة يظهر رمز بنكي عام. يمكن لاحقًا استبدال الـ badge النصي بشعار SVG رسمي لكل جهة إذا تم توفير ملفات الشعارات أو روابطها الموثوقة.
