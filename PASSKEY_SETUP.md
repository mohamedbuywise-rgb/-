# Passkey Setup for دبّر

تمت إضافة دعم Passkey إلى صفحة `public/app/dabbar-onboarding.html` باستخدام واجهات Supabase Auth التجريبية الرسمية.

## ما تم تنفيذه

بعد تسجيل الدخول بالبريد وكلمة المرور، يظهر للمستخدم خيار اختياري لتسجيل Passkey باستخدام بصمة الجهاز أو Face ID أو رمز الجهاز أو مفتاح أمان مدعوم. بعد التسجيل الناجح، ينتقل المستخدم إلى تدفق تثبيت التطبيق المعتاد. كما تمت إضافة زر مستقل لتسجيل الدخول مباشرة باستخدام Passkey دون إدخال البريد أو كلمة المرور.

إذا ألغى المستخدم نافذة النظام أو لم ينجح التسجيل، يظل مسار البريد وكلمة المرور متاحًا ولا يتوقف onboarding. وإذا كان المتصفح لا يدعم WebAuthn أو الصفحة ليست ضمن HTTPS، يتم إخفاء خيارات Passkey تلقائيًا.

## تفعيل الميزة في Supabase

الميزة تعتمد على إعدادات مشروع Supabase، ولا يمكن تفعيلها من الواجهة الأمامية باستخدام المفتاح العام وحده:

1. افتح **Authentication → Passkeys** في لوحة تحكم Supabase.
2. فعّل **Enable Passkey authentication**.
3. اجعل **Relying Party Display Name** هو `دبّر`.
4. اجعل **Relying Party ID** هو النطاق الأساسي للإنتاج، مثل `example.com`، من دون `https://` أو مسار.
5. أضف **Relying Party Origins** التي يستخدمها التطبيق، مثل `https://example.com`، ويمكن إضافة نطاق المعاينة عند الحاجة.
6. تأكد من أن **Site URL** وRedirect URLs في Supabase تسمحان بعنوان صفحة onboarding.

> يجب تثبيت Relying Party ID قبل بدء تسجيل المستخدمين؛ تغييره لاحقًا يجعل Passkeys المسجلة سابقًا غير صالحة.

## متطلبات التشغيل

يجب فتح التطبيق عبر HTTPS في الإنتاج. تعمل Passkeys على المتصفحات والأجهزة التي تدعم WebAuthn، وقد يظهر للمستخدم اختيار بين البصمة أو Face ID أو PIN أو مفتاح أمان بحسب نظام التشغيل ومدير كلمات المرور الموجود على الجهاز.

تستخدم الصفحة نسخة Supabase JS `2.105.0` أو أحدث مع تفعيل `auth.experimental.passkey: true` عند إنشاء العميل، لأن دعم Passkeys في Supabase ما زال مصنفًا كتجريبي وقد تتغير واجهاته مستقبلًا.

## اختبار القبول

اختبر السيناريوهات التالية على جهاز حقيقي أو محاكي يدعم WebAuthn:

- تسجيل حساب مؤكد بالبريد ثم تسجيل Passkey بنجاح.
- تسجيل الخروج ثم استخدام زر **الدخول بالبصمة أو Passkey**.
- إلغاء نافذة النظام والتأكد من ظهور رسالة مفهومة مع بقاء تسجيل الدخول التقليدي متاحًا.
- فتح الصفحة على متصفح غير داعم أو عبر HTTP والتأكد من عدم ظهور زر Passkey.
- إعادة تثبيت التطبيق ثم تسجيل الدخول باستخدام Passkey، مع التأكد من استمرار مسار كلمة المرور كخطة بديلة.
