# Unified Expense Entry

تمت إضافة بطاقة «أضف مصروفك بسرعة» داخل تبويب تقرير نهاية الشهر، بعد Active Days وقبل إجمالي الشهر.

## المسارات

- **كتابة:** يرسل النص إلى `POST /api/assistant` مع `action=entry_draft`.
- **صوت:** يسجل المتصفح WebM لمدة أقصاها 30 ثانية، يرسله إلى Groq Whisper، ثم يمرر النص إلى نفس محلل التسجيل.
- **فاتورة:** زر البطاقة يفتح عنصر الكاميرا الموجود أصلًا ويستخدم مسار Smart Receipt Scanner والـanimation الحالي.

## الأمان والدقة

الـAI يخرج مسودة فقط: `expense` أو `debt` أو `invoice`. لا يتم الحفظ من الصوت أو النص مباشرة. تظهر المسودة للمستخدم ويجب الضغط على «تأكيد التسجيل». المصروفات والديون تحفظ في الجداول الصحيحة، والفاتورة تستخدم `saveInvoiceRecord` المشترك حتى تظل مرتبطة بالأصناف والفواتير الحالية.

لا يوجد ضمان واقعي لدقة تفريغ 100% أو 98% لكل اللهجات والتسجيلات. لذلك يوجد حد للصوت، ومعاينة قبل الحفظ، وتحذير عند انخفاض confidence، وخيار تعديل أو إعادة التسجيل.

## الاختبارات

نجح فحص JavaScript المضمن في الداشبورد، وفحص الصياغة لكل ملفات `api/` و`lib/` وعددها 33 ملفًا، وفحص وجود مساري `entry_draft` و`entry_confirm` و18 عنصرًا متعلقًا بالبطاقة.

## النشر

لا توجد Migration إضافية لهذه البطاقة إذا كانت Migration `global-context.sql` قد شُغّلت بالفعل؛ فهي تضيف `currency_code` المطلوب للعمليات. يجب نشر النسخة كاملة، ثم اختبار الكتابة والصوت والفاتورة بحساب تجريبي، والتأكد من ظهور رسالة النجاح وتحديث الأرقام بعد الحفظ.
