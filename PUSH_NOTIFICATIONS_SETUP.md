# إعداد إشعارات دبّر

## 1. تثبيت قاعدة البيانات

شغّل ملف `sql/push-notifications.sql` في Supabase SQL Editor مرة واحدة. الملف ينشئ جداول اشتراكات الأجهزة، تفضيلات المستخدمين، وسجل منع تكرار الإشعارات.

## 2. إضافة متغيرات البيئة

أضف المتغيرات التالية إلى بيئة النشر:

```env
VAPID_SUBJECT=mailto:your-email@example.com
VAPID_PUBLIC_KEY=...
VAPID_PRIVATE_KEY=...
```

لتوليد زوج مفاتيح جديد محليًا بعد تثبيت الحزم:

```bash
pnpm exec web-push generate-vapid-keys --json
```

ضع `publicKey` في `VAPID_PUBLIC_KEY` و`privateKey` في `VAPID_PRIVATE_KEY`. المفتاح الخاص لا يوضع في ملفات الواجهة أو المستودع.

تظل المتغيرات الحالية مثل `SUPABASE_URL` و`SUPABASE_SERVICE_ROLE_KEY` و`CRON_SECRET` مطلوبة كما هي.

## 3. النشر

بعد نشر المشروع، افتح الداشبورد على HTTPS، ادخل إلى تبويب «حسابي»، واضغط «تفعيل» في كارت «إشعارات دبّر». المتصفح سيطلب الإذن، وبعد الموافقة يتم حفظ اشتراك الجهاز في Supabase.

الإشعارات تعمل لكل جهاز/متصفح على حدة. إيقافها من جهاز لا يوقفها تلقائيًا على جهاز آخر.

## 4. ما الذي يرسل الإشعارات؟

كل مستخدم يختار بنفسه **وقت التذكير (ساعة ودقيقة)** و**نسبة تنبيه الميزانية (من 50% إلى 100%)** من كارت «إشعارات دبّر»، ويُحفظ معهما توقيت جهازه (`timezone`) فيصله كل إشعار في وقته المحلي.

- التذكير اليومي: في الوقت الذي اختاره المستخدم (الافتراضي 8:00 صباحًا)، إذا لم توجد مصروفات مسجلة في اليوم الحالي.
- ملخص نهاية اليوم: بعد منتصف الليل (12:00 ص) بتوقيت المستخدم عن اليوم السابق، ويمكن إيقافه منفصلًا.
- الملخص الأسبوعي: صباح السبت في وقت التذكير الذي اختاره المستخدم، إذا كان الخيار مفعّلًا.
- حركات البنك (SMS): إشعار فوري لكل حركة بنكية تصل عبر الـ SMS (إيداع أو خصم، المبلغ، الجهة، الرصيد)، عبر الدالة `sendBankMovementPush` في `lib/webPush.js` (تُستدعى من `sms-webhook.js` بعد تسجيل الحركة). يمكن إيقافها من إعدادات الإشعارات.
- تذكير مواعيد الدفع: عند اقتراب موعد أي تذكير مسجّل (يومين قبل / يوم قبل / يوم الاستحقاق) في وقت التذكير الذي اختاره المستخدم، مرة واحدة لكل مرحلة. يمكن إيقافه من إعدادات الإشعارات.
- تنبيه الميزانية: يصدر بعد تسجيل أو تعديل مصروف عندما يصل إجمالي الميزانية الشهرية أو ميزانية فئة إلى النسبة التي اختارها المستخدم، مرة واحدة لكل شهر ولكل ميزانية.

## 5. الجدولة لكل مستخدم (مهم)

Vercel Hobby يسمح بكرون واحد فقط في اليوم، لذلك الجدولة الدقيقة تتم بهذه الطريقة:

1. `/api/push-cron` هو endpoint مخصص (الكود في `backend/api-handlers/push-cron.js` و`lib/pushSchedule.js`). كل مرة يُستدعى يفحص: من المستخدمين حان وقته المحلي؟ ويرسل له. جدول `push_notification_runs` يمنع التكرار، وأي إشعار لا يُرسل إذا تأخر أكثر من 60 دقيقة عن ميعاده.
2. الاستدعاء كل 5 دقائق يتم من خارج Vercel. الطريقة الموصى بها: **Supabase pg_cron** عبر `sql/push-scheduler.sql`.
3. `/api/cron-daily` (جدولي `vercel.json` اليوميين) ما زال يشغّل نفس الدالة كاحتياطي، فلو توقف الـ scheduler الخارجي يستمر الإرسال بدقة أقل.

الخطوات:

```bash
# اختبار يدوي بعد النشر (لازم يرجع ok:true)
curl -H "Authorization: Bearer $CRON_SECRET" https://YOUR-DOMAIN/api/push-cron
```

ثم شغّل `sql/push-scheduler.sql` في Supabase SQL Editor بعد استبدال `YOUR-DOMAIN` و`YOUR_CRON_SECRET`.

**بديل بدون Supabase:** أنشئ مهمة في cron-job.org كل 5 دقائق على `https://YOUR-DOMAIN/api/push-cron` مع Header باسم `Authorization` وقيمته `Bearer <CRON_SECRET>`.

`CRON_SECRET` إلزامي لهذا الـ endpoint (يرجع 503 إذا لم يُضبط). دقة الوصول تساوي تكرار الـ scheduler: كل 5 دقائق تعني وصولًا خلال 5 دقائق من الوقت المختار. أخطاء الاشتراكات المنتهية 404/410 تعطل الاشتراك تلقائيًا.

## 6. تشخيص الإشعارات

- **زر "🔔 جرّب إشعار الآن"** في كارت إشعارات دبّر: بيبعت إشعار فعلي من الخادم لجهازك ويعرض النتيجة (وصل / سبب الفشل). كمان الداشبورد بيصلّح تلقائيًا أي اختلاف بين اشتراك المتصفح واشتراك الخادم (اشتراك ناقص، أو معطّل، أو بمفاتيح VAPID قديمة) كل ما تفتحه.
- **`GET /api/push-cron?debug=1`** (مع `Authorization: Bearer <CRON_SECRET>`): بيرجّع لكل مستخدم نشط (آخر 4 أرقام بس) توقيته المحلي ووقت التذكير وإيه اللي مستحق دلوقتي ونتيجة كل إرسال.
- الإرسال بقى `urgency: high` للحركات البنكية والتذكيرات والتنبيهات الفورية، عشان أندرويد ما يأخّرهاش لما الموبايل يبقى خامل.
- لو الإرسال فشل، حجز "مرة واحدة في اليوم" بيتحرر تلقائيًا فالمحاولة الجاية بتنجح.

## 7. رسائل البنوك والمحافظ (SMS)

- `lib/smsParser.js`: محلل قواعد عربي/إنجليزي (المبلغ الحقيقي مش الرصيد/الرسوم، الاتجاه، التاجر/الطرف الآخر). لو واثق بيسجّل مباشرة من غير AI؛ لو مش واثق بيسأل الـ AI (برومبت مخصوص) ويراجع ناتجه.
- `lib/bank-senders.js`: قايمة موسّعة بالبنوك والمحافظ المصرية + التعرّف على الجهة من نص الرسالة لو اسم المرسل غير معروف.
- **التكرار:** بصمة SHA-256 لنص الرسالة كامل؛ نفس النص بالحرف خلال 90 ثانية فقط يُعتبر وصولًا مزدوجًا. أي رسالة تانية (حتى بنفس المبلغ والتاجر) بتتسجل عادي. شغّل `sql/sms-dedupe.sql` مرة واحدة.
- **الماكرو الجديد** بيبعت نص عادي (`token=`/`sender=`/`text=`) بدل JSON، لأن نص الرسالة الحقيقي فيه سطور جديدة وعلامات اقتباس كانت بتكسر الـ JSON. حمّله من الداشبورد (كارت البنوك) واستورده في MacroDroid؛ السيرفر لسه بيقبل القديم.
- **اختبار من غير تسجيل:** ابعت `"dry_run": true` في الطلب وهيرجّع نتيجة التحليل من غير ما يسجل أو يبعت إشعار.
- **الحركات البنكية:** أي سحب/إيداع/تحويل بيظهر فيها (حتى لو اتسجل من غير SMS)، والتحويل بيتسجل باتجاهه الحقيقي (+/−) وما بيتحسبش مصروف ولا دخل.
