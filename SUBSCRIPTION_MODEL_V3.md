# نموذج الاشتراك والصلاحيات في دبّر

## الخلاصة التنفيذية

يعتمد النموذج على ثلاث حالات واضحة للمستخدم: **تجربة مجانية**، و**اشتراك فعّال**، و**مجاني مقفول**. تبدأ التجربة من أول استخدام فعلي لميزة أو إدخال، وليس من إنشاء الحساب أو إرسال `/start`. تستمر التجربة 3 أيام كاملة. بعد انتهائها تظل البيانات التاريخية متاحة، ويستمر الإدخال اليدوي مع اختيار الفئة يدويًا، بينما تُقفل الميزات التي تستهلك خدمات الذكاء الاصطناعي أو تحتاج ربطًا خارجيًا.

طبّق التنفيذ بوابة صلاحيات مركزية في `lib/subscriptionAccess.js`. كل نقطة دخول، سواء Telegram أو الداشبورد أو Share Extension أو SMS البنكي، تستعمل هذه السياسة قبل تنفيذ الفعل المكلف أو المقفول.

## حالات المستخدم

| الحالة | شرط الدخول | الميزات المتاحة | الميزات المقفولة |
|---|---|---|---|
| `trial` | بدأ أول استخدام فعلي ولم تمر 3 أيام | كل الميزات، مع حدود استخدام عادلة | لا شيء، باستثناء تجاوز الحد اليومي |
| `subscribed` | `subscription_expires_at` في المستقبل | كل الميزات، مع حدود شهرية | لا شيء، باستثناء تجاوز الحد الشهري |
| `free_locked` | انتهت التجربة ولا يوجد اشتراك فعّال | الإدخال اليدوي، اختيار الفئة يدويًا، قراءة التاريخ والتقارير المتاحة | الصوت، OCR، ربط البنوك، اسأل دبّر، Share Extension، التصنيف التلقائي بالـ AI |

> **قاعدة حفظ البيانات:** حالة الاشتراك تتحكم في تنفيذ الفعل الجديد فقط. لا توجد أي عملية حذف أو إخفاء أو فلترة للمعاملات أو التصنيفات أو سجل المستخدم بسبب انتهاء التجربة أو الاشتراك.

## تدفق المستخدم

### التسجيل والاستخدام الأول

ينشئ `/start` أو تسجيل الحساب صف المستخدم فقط. لا يُستهلك وقت التجربة في هذه المرحلة. عند أول استخدام فعلي، تستدعي البوابة `getFeatureAccess(..., { startTrial: true })`، فتسجل `trial_started_at` ذريًا وتحدد نهاية التجربة بعد 72 ساعة.

خلال التجربة تظهر كل الميزات في التطبيق وTelegram. كل استدعاء مكلف يمر عبر `usage_counters`، بحيث لا يستطيع المستخدم تجاوز السقف بسبب طلبين متزامنين.

### التنبيه قبل الانتهاء

يبحث `cron-daily` مرة يوميًا عن المستخدمين الذين بدأوا التجربة قبل يومين تقريبًا ولم تُرسل لهم رسالة تذكير. يرسل تنبيهًا يذكر الميزات التي ستُقفل وسعر الاشتراك، ثم يضع `trial_reminder_sent_at`. العملية idempotent ولا تعيد إرسال التنبيه لنفس التجربة.

### بعد انتهاء التجربة

عند محاولة استخدام ميزة مقفولة، تعيد API استجابة `403` تحتوي على `subscriptionRequired: true` و`subscriptionUrl` ورسالة عربية واضحة. في Telegram تظهر رسالة مباشرة مع توجيه إلى `/subscription`.

أما النص اليدوي فيستعمل parser حتميًا لا يستدعي أي AI. الصيغ الأساسية المدعومة هي مثل `مصروف 50 أكل` و`دخل 500 مرتب`. اختيار الفئة يتم من قائمة `CATEGORIES` الموجودة مسبقًا، ويظل السجل محفوظًا كالمعتاد.

### الاشتراك والتجديد

عند تفعيل الاشتراك، يظل `subscription_expires_at` هو مصدر الحقيقة للوصول. إذا كان الاشتراك السابق ما زال فعّالًا، يُضاف الشهر إلى تاريخ الانتهاء الحالي بدلًا من ضياع الأيام المتبقية. بمجرد أن يصبح التاريخ في المستقبل تعود جميع الميزات فورًا.

## تصميم قاعدة البيانات

### أعمدة `users`

- `trial_started_at`: بداية أول استخدام فعلي. Nullable حتى لا يبدأ `/start` التجربة.
- `trial_reminder_sent_at`: يمنع تكرار تنبيه اليوم السابق لانتهاء التجربة.
- `subscription_expires_at`: نهاية الاشتراك المدفوع الحالي. الاشتراك فعّال عندما تكون القيمة في المستقبل.

### جدول `subscription_events`

يسجل انتقالات الحالة ومحاولات استخدام الميزات. أهم القيم هي `trial_started`، و`trial_ended`، و`subscription_started`، و`subscription_renewed`، و`subscription_expired`، و`feature_blocked`، و`feature_used`. هذا السجل لا يستبدل جداول المصروفات؛ وظيفته التدقيق والتحليل فقط.

### جدول `subscription_plans`

يحتوي خطة `monthly` بسعر 199 جنيهًا مصريًا وحدود الاستخدام الشهرية. وضع الحدود في JSON يجعل تعديلها ممكنًا من قاعدة البيانات دون إعادة نشر التطبيق، مع إبقاء القيم الافتراضية في `lib/config.js` كخطة احتياطية.

### عدادات الاستخدام

يستمر `usage_counters` في استعمال مفتاح فترة مستقل لكل مستخدم. التجربة تستعمل فترة يومية، والاشتراك يستعمل فترة شهرية. لا تُحذف العدادات القديمة تلقائيًا، ويمكن الاحتفاظ بها للتحليل المالي والتشغيلي.

## حدود الاستخدام المقترحة

| الميزة | التجربة، يوميًا | الاشتراك، شهريًا | المبرر |
|---|---:|---:|---|
| التفريغ الصوتي | 15 | 250 | حماية تكلفة التفريغ مع تجربة سخية |
| قراءة الفواتير OCR | 3 | 50 | الصورة أعلى تكلفة من النص |
| أسئلة المساعد | 50 | 180 | يسمح بتجربة حقيقية دون فتح استهلاك غير محدود |
| التصنيف التلقائي للنص | 15 | 300 | يغطي الاستخدام المعتاد ويمنع إساءة الاستخدام |
| كشوف الحساب البنكية | 2 | 10 | كل كشف قد يحتوي دفعات كثيرة من استدعاءات AI |

هذه الحدود قابلة للتعديل. عند الفشل الفني الذي لا يسببه المستخدم، يعاد العداد في OCR وكشف الحساب باستخدام دوال refund الحالية.

## نقاط التكامل المنفذة

- `lib/subscriptionAccess.js`: مصدر السياسة الموحد للحالات والصلاحيات ورسائل الاشتراك.
- `sql/subscription-model-v3.sql`: migration آمنة، تضيف حالة الاستخدام الأول، سجل الأحداث، الخطط، ودالة بدء التجربة الذرية.
- `telegram-webhook.js`: يقفل الصوت والفواتير بعد التجربة، ويتيح الإدخال اليدوي غير المعتمد على AI.
- `assistant.js`: يفرض صلاحية المساعد على مسارات الداشبورد التي تستدعي ميزات دبّر الذكية.
- `bank-accounts.js` و`bank-statement-import.js` و`sms-webhook.js`: تفرض صلاحية ربط البنوك قبل إنشاء أو معالجة مسار البنك.
- `quick-add-webhook.js`: يقفل Share Extension بعد التجربة، بما يشمل النص والصوت والصورة القادمة من تطبيق آخر.
- `cron-daily.js`: يرسل تذكيرًا قبل انتهاء التجربة بيوم تقريبًا مع منع التكرار.

## قواعد الاختبار قبل النشر

1. مستخدم جديد يرسل `/start` ثم ينتظر؛ يجب ألا ينقص ذلك من التجربة.
2. أول إدخال فعلي يسجل `trial_started_at` مرة واحدة فقط حتى مع طلبين متزامنين.
3. مستخدم في `trial` يستطيع استخدام الصوت وOCR والمساعد والبنوك وShare Extension وTelegram ضمن الحدود.
4. مستخدم في `free_locked` يستطيع تسجيل `مصروف 50 أكل` و`دخل 500 مرتب` دون أي نداء AI.
5. مستخدم في `free_locked` يحصل على `403` ورابط اشتراك عند طلب الصوت أو OCR أو المساعد أو البنك أو المشاركة.
6. انتهاء الاشتراك لا يغير نتيجة استعلامات التاريخ ولا يحذف أي صف من `expenses` أو `financial_events` أو `chat_messages` أو `invoices`.
7. تفعيل الاشتراك يعيد الوصول فورًا، ويبدأ التمديد من تاريخ الانتهاء الحالي إذا كان ما زال مستقبلًا.
8. كرون التنبيه لا يرسل الرسالة مرتين لنفس المستخدم، ولا يرسلها لمستخدم اشترك قبل موعدها.

## التشغيل

شغّل `sql/subscription-model-v3.sql` في Supabase SQL Editor مرة واحدة. بعد ذلك انشر ملفات الخادم. لا يحتاج التطبيق إلى migration لحذف أو نقل البيانات المالية؛ التغيير يضيف أعمدة وسياسة وصول فقط.

## References

[1]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase Row Level Security documentation"
[2]: https://www.postgresql.org/docs/current/sql-createfunction.html "PostgreSQL CREATE FUNCTION documentation"
